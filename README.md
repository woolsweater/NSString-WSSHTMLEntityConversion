NSString+WSSHTMLConversion

Category on NSString to decode HTML entities in the string into their appropriate Unicode points.

Doesn't cover all possible HTML entities, but it is quite easy to add more.